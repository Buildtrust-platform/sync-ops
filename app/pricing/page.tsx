import PricingPage from '@/app/components/PricingPage';

export const metadata = {
  title: 'Pricing | SyncOps',
  description: 'Simple, transparent pricing for production teams of all sizes. Start free, scale as you grow.',
};

export default function PricingRoute() {
  return <PricingPage />;
}
